# Shiny Server Configuration for KEGG Pathway Visualizer - Performance Optimized
run_as shiny;

server {
  listen 3838;

  location / {
    site_dir /srv/shiny-server/kegg-app;
    log_dir /var/log/shiny-server;
    
    # Extended timeouts for KEGG data loading and bioinformatics processing
    app_init_timeout 1200;
    app_idle_timeout 600;
  }
}
